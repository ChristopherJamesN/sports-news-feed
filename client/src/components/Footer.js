import React from 'react';
import { Panel } from 'react-bootstrap';

const Footer = () => {
  return (
    <Panel
      header={'Thanks to https://newsapi.org/ for the API.'}
      bsStyle="default"
    ></Panel>
  );
};

export default Footer;
